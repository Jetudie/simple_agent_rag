# Pass Criteria

Replace or extend these criteria for your targets. The independent verifier checks every item and fails
closed when evidence is missing.

- Every requirement in `GOALS.md` is implemented in the workspace.
- The documented primary usage path runs successfully.
- Relevant automated tests pass.
- No known critical errors, placeholder implementations, or unresolved required TODOs remain.

